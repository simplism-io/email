import { IOptions } from "./types/config";
declare var EventEmitter: any;
export declare class MailListener extends EventEmitter {
    constructor(options: IOptions);
    start(): void;
    stop(): void;
    imapReady(): void;
    imapClose(): number;
    imapError(error: any): void;
    imapMail(): void;
    parseUnread(): void;
}
export {};
